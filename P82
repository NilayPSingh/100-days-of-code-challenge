#include <stdio.h>

enum Light{RED,YELLOW,GREEN};

int main(){
    enum Light l=RED;
    if(l==RED) printf("Stop\n");
    l=YELLOW;
    if(l==YELLOW) printf("Wait\n");
    l=GREEN;
    if(l==GREEN) printf("Go\n");
    return 0;
}
