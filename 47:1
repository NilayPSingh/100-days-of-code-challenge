#include <stdio.h>
#include <string.h>

int main() {
    char str1[100], str2[100];
    int freq1[26] = {0}, freq2[26] = {0};
    scanf("%[^\n]", str1);
    getchar();
    scanf("%[^\n]", str2);

    for(int i = 0; str1[i] != '\0'; i++) {
        if(str1[i]>='a' && str1[i]<='z') freq1[str1[i]-'a']++;
        else if(str1[i]>='A' && str1[i]<='Z') freq1[str1[i]-'A']++;
    }

    for(int i = 0; str2[i] != '\0'; i++) {
        if(str2[i]>='a' && str2[i]<='z') freq2[str2[i]-'a']++;
        else if(str2[i]>='A' && str2[i]<='Z') freq2[str2[i]-'A']++;
    }

    int flag = 1;
    for(int i = 0; i < 26; i++) {
        if(freq1[i] != freq2[i]) {
            flag = 0;
            break;
        }
    }

    if(flag) printf("Anagrams");
    else printf("Not Anagrams");

    return 0;
}
