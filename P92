#include <stdio.h>

struct Student{ char name[100]; int roll_no; int marks; };

int main(){
    struct Student a[5];
    for(int i=0;i<5;i++) scanf("%99s %d %d",a[i].name,&a[i].roll_no,&a[i].marks);
    for(int i=0;i<5;i++) printf("%s %d %d\n",a[i].name,a[i].roll_no,a[i].marks);
    return 0;
}

