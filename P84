#include <stdio.h>

enum Status{SUCCESS,FAILURE,TIMEOUT};

int main(){
    enum Status s=SUCCESS;
    if(s==SUCCESS) printf("Success\n");
    s=FAILURE;
    if(s==FAILURE) printf("Failure\n");
    s=TIMEOUT;
    if(s==TIMEOUT) printf("Timeout\n");
    return 0;
}
