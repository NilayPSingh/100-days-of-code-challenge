#include <stdio.h>

struct Student{ char name[100]; int roll_no; int marks; };

int main(){
    struct Student s;
    scanf("%99s %d %d",s.name,&s.roll_no,&s.marks);
    printf("%s %d %d\n",s.name,s.roll_no,s.marks);
    return 0;
}
