#include <stdio.h>

enum Role{ADMIN,USER,GUEST};

int main(){
    int r;
    if(scanf("%d",&r)!=1) return 0;
    enum Role role=r;
    if(role==ADMIN) printf("Admin access\n");
    else if(role==USER) printf("User access\n");
    else if(role==GUEST) printf("Guest access\n");
    else printf("Unknown role\n");
    return 0;
}
