#include <stdio.h>
#include <stdlib.h>

int main(){
    int n;
    if(scanf("%d",&n)!=1) return 0;
    int *a=(int*)malloc(n*sizeof(int));
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    int *ans=(int*)malloc(n*sizeof(int));
    long long *pref=(long long*)malloc(n*sizeof(long long));
    long long prod=1;
    pref[0]=1;
    for(int i=0;i<n;i++){
        if(i==0) pref[i]=1; else pref[i]=pref[i-1]*a[i-1];
    }
    long long suff=1;
    for(int i=n-1;i>=0;i--){
        ans[i]=(int)(pref[i]*suff);
        suff *= a[i];
    }
    for(int i=0;i<n;i++){
        if(i) printf(" ");
        printf("%d",ans[i]);
    }
    printf("\n");
    free(a); free(ans); free(pref);
    return 0;
}
